<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  
  <!-- Local Parent -->
  <!--<parent>
    <artifactId>parent</artifactId>
    <groupId>com.reneekbartlett</groupId>
    <version>1.0.0</version>
    <relativePath>../parent/pom.xml</relativePath>
  </parent>-->

  <groupId>com.reneekbartlett</groupId>
  <artifactId>verisimilar</artifactId>
  <version>1.0.0-SNAPSHOT</version>
  <packaging>pom</packaging>

  <name>verisimilar</name>

  <!-- Verisimilar Modules -->
  <modules>
    <!-- Core Library for Data Generation -->
    <module>core</module>
    <!-- API Library with Endpoints (Spring Boot) -->
    <module>api</module>
  </modules>

  <properties>

        <!-- TODO -->
        <verisimilar.environment.name>dev-instance</verisimilar.environment.name>
        <verisimilar.core.config.dir>../../parent/config/verisimilar/core/</verisimilar.core.config.dir>
        <verisimilar.api.config.dir>../../parent/config/verisimilar/api/</verisimilar.api.config.dir>
        <application.security.allow-url-api-keys>true</application.security.allow-url-api-keys>

        <java.version>21</java.version>
        <maven.compiler.release>${java.version}</maven.compiler.release>
        <maven.compiler.source>${java.version}</maven.compiler.source>
        <maven.compiler.target>${java.version}</maven.compiler.target>

        <build.plugins.plugin.version>21</build.plugins.plugin.version>

        <main.basedir>${basedir}/../..</main.basedir>

        <m2eclipse.wtp.contextRoot>/</m2eclipse.wtp.contextRoot>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <spring-boot-version>4.0.2</spring-boot-version>

        <!-- Use 2.0.5+ for Spring Boot 4 -->
        <testcontainers.version>2.0.5</testcontainers.version>
        <joinfaces.version>6.0.2</joinfaces.version>
    </properties>

    <profiles>
        <profile>
            <id>dev</id>
            <properties>
                <application.security.allow-url-api-keys>true</application.security.allow-url-api-keys>
            </properties>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>
        </profile>

        <profile>
            <id>test</id>
            <properties>
                <verisimilar.environment.name>test-instance</verisimilar.environment.name>
                <verisimilar.core.config.dir>../../parent/config/verisimilar/core/</verisimilar.core.config.dir>
                <verisimilar.api.config.dir>../../parent/config/verisimilar/api/</verisimilar.api.config.dir>

                <application.security.allow-url-api-keys>true</application.security.allow-url-api-keys>

                <!-- Hikari tuning for Testcontainers -->
                <spring.datasource.hikari.max-lifetime>30000</spring.datasource.hikari.max-lifetime>
                <spring.datasource.hikari.idle-timeout>10000</spring.datasource.hikari.idle-timeout>
                <spring.datasource.hikari.validation-timeout>2500</spring.datasource.hikari.validation-timeout>
                <spring.datasource.hikari.minimum-idle>1</spring.datasource.hikari.minimum-idle>
                <spring.datasource.hikari.connection-timeout>5000</spring.datasource.hikari.connection-timeout>
            </properties>
        </profile>
    </profiles>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot-version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>

        <dependency>
            <groupId>tools.jackson.core</groupId>
            <artifactId>jackson-core</artifactId>
        </dependency>
        <dependency>
            <groupId>tools.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
        </dependency>
        <dependency>
            <groupId>tools.jackson.datatype</groupId>
            <artifactId>jackson-datatype-json-org</artifactId>
        </dependency>
        <!-- Add on module for Java 8 Date and Time Data Types -->
        <dependency>
            <groupId>tools.jackson.datatype</groupId>
            <artifactId>jackson-datatype-jsr310</artifactId>
            <version>3.0.0-rc2</version>
        </dependency>
        <dependency>
            <groupId>tools.jackson.dataformat</groupId>
            <artifactId>jackson-dataformat-csv</artifactId>
        </dependency>

        <dependency>
            <groupId>net.logstash.logback</groupId>
            <artifactId>logstash-logback-encoder</artifactId>
            <version>9.0</version>
        </dependency>
        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
            <!-- <version>1.5.12</version> Use 1.3.x for JDK 8; 1.5.x for JDK 11+ -->
        </dependency>

        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-lang3</artifactId>
            <version>3.20.0</version>
        </dependency>

        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-text</artifactId>
            <version>1.15.0</version>
        </dependency>

        <dependency>
            <groupId>org.xerial</groupId>
            <artifactId>sqlite-jdbc</artifactId>
            <!--<version>3.53.2.0</version>-->
            <scope>compile</scope>
        </dependency>

        <!-- TODO: REMOVE? -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- FOR TESTING -->
        <!--<dependency>
            <groupId>com.googlecode.libphonenumber</groupId>
            <artifactId>libphonenumber</artifactId>
            <version>9.0.26</version>
        </dependency>-->

        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers-junit-jupiter</artifactId>
            <version>${testcontainers.version}</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.openjdk.jmh</groupId>
            <artifactId>jmh-core</artifactId>
            <version>1.37</version>
            <scope>test</scope>
        </dependency>

    </dependencies>

    <build>
        <!--<sourceDirectory>src</sourceDirectory>-->
        <!--<testSourceDirectory>test</testSourceDirectory>-->
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <configuration>
                        <release>${java.version}</release>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <configuration>
                        <useSystemClassLoader>false</useSystemClassLoader>
                        <skipTests>false</skipTests>
                        <!--<argLine>-javaagent:${org.mockito:mockito-core:jar} -Xshare:off</argLine>-->
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

    
</project>
